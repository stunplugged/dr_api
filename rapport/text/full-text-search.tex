I videoapplikation vil der være behov for at kunne søge efter videoer eller programserier ud fra specifikke ord, som kan være en del af titlen eller fx beskrivelsen, som er den del der indeholder mest tekst.  

\section{Oprettelse af full-text index}

I BaseX databasen er det muligt at oprette et "full-text index" til sin XML database. Index’et vil bliver oprettet på indhold af alle elementer i XML dokumentet og ikke kun på et specifikt element i dokumentet. Sammenlignes dette med en relationel database vil det svare til, at index’et ikke kun oprettes på en specifik kolonne i en specifik tabel, men på data i alle kolonner i alle tabeller i databasen. 

\subsection{Indstillinger til full-text index}

Ved oprettelsen af index’et skal sproget angives, ellers sættes det default til engelsk. I denne videoapplikation vil sproget være dansk og det får stor betydning når teksterne i databasen udsættes for ”Stemming”. Stemming er en proces, som reducere ord ned til deres mindste form. Det vil fx sige at et ord som ”Bilerne” vil blive til ”bil”, før det gemmes som en værdi i index’et. Der kan også tilføjes en "stop word list", som er en liste af ord, der ikke ønskes medtaget i index'et.  

\section{Full-text søgning}

Det skal i videoapplikationen være muligt at søge efter specifikke ord inden for en specifik kategori af programserier. Det vil sige at full-text søgningen skal kombineres med en XQuery-forespørgelse, hvor kun elementer, som indgår i den efterspurgte kategori af programserier medtages og kun returneres som resultat, hvis de også kan opfylde full-text søgningen. 
Denne søgning ses i figur \ref{fulltextSearch:programserierLivsstilOrd}.

\begin{figure}[ht]
%\centering
\begin{lstlisting}[style=FAKE_XQUERY, language=XQUERY]
//ArrayOfProgramSerie/ProgramSerie/Labels[string='livsstil']/..
         /Description[ft:contains(text(), ("restaurant", "laks"), { "mode": "any" })]/..
\end{lstlisting}
\caption{Implementationen af søgning efter specifik kategori af programserier med specifikke ord som indhold.}
\label{fulltextSearch:programserierLivsstilOrd}
\end{figure}

\section{En sky af ord}

Til videoapplikationen ønskes et flot baggrundsbillede, som består af en sky af de mest brugte ord i alle elementerne, som beskriver programserierne. Til at skabe sådan et billede skal full-text index’et undersøges. 
Indholdet skal tages ud i en rækkefølge, hvor de mest brugte ord kommer først og hvor der med hvert ord følger et tal, som beskriver hvor mange gange ordet er anvendt.
I figur \ref{fulltext:beforeStopWords} ses et eksempel på, hvordan det kommer til at se ud når der dannes et billede af ordene i index'et. 
Billedet i figuren illustrere dog også et lille problem, da ordene er simple ord som "og", "der", "at" osv. Dette løses ved at indføre en "stop word list".

\begin{figure}[ht]
  \centering
   \includegraphics[width=1.1\textwidth]{pic/wordcloud1b.png}
   \caption{Eksempel på billede før indførelse af "stop word list".}
   \label{fulltext:beforeStopWords}
\end{figure}

\subsection{Tilføjelse af stop word list}

Da ikke alle ord er lige interesant, at have med i index’et, så er det muligt at oprette en "stop word list", som indeholder alle de ord, som skal undtages når et full-text index’et oprettes. 
I dette projekt er denne "stop word list" blevet oprettet ved at undersøge de meste brugte ord i index'et uden "stop word list" og derefter tilføje dem til listen, som ikke giver mening i et full-text index.
I figur \ref{fulltext:afterStopWords} ses nu hvordan udvalget af ord er blevet bedre.

\begin{figure}[ht]
  \centering
   \includegraphics[width=1.1\textwidth]{pic/wordcloud2c.png}
   \caption{Eksempel på billede efter indførelse af "stop word list".}
   \label{fulltext:afterStopWords}
\end{figure}